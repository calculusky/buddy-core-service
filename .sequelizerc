const path = require("path");

module.exports = {
    config: path.resolve("sequelize", "config.js"),
    "models-path": path.resolve("build", "models", "sql"),
    "seeders-path": path.resolve("sequelize", "seeders"),
    "migrations-path": path.resolve("sequelize", "migrations"),
};
